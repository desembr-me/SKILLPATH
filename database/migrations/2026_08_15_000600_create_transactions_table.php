<?php
use Illuminate\Database\Migrations\Migration; use Illuminate\Database\Schema\Blueprint; use Illuminate\Support\Facades\Schema;
return new class extends Migration { public function up():void{Schema::create('transactions',function(Blueprint $t){$t->id();$t->foreignId('parent_id')->constrained('users')->cascadeOnDelete();$t->foreignId('enrollment_id')->constrained()->cascadeOnDelete();$t->string('invoice_code')->unique();$t->decimal('subtotal',12,2);$t->decimal('platform_fee',12,2)->default(15000);$t->decimal('total',12,2);$t->string('payment_method')->nullable();$t->enum('status',['pending','paid','failed','expired','refunded'])->default('pending');$t->timestamp('paid_at')->nullable();$t->json('metadata')->nullable();$t->timestamps();});} public function down():void{Schema::dropIfExists('transactions');} };
