@extends('layouts.app')
@section('title',$instructor->name.' | Pengajar SKILLPATH')
@section('content')<section class="instructor-profile-hero"><div class="container instructor-profile-grid"><div class="teacher-avatar hero-avatar">{{ strtoupper(substr($instructor->name,0,1)) }}</div><div><span class="eyebrow">Pengajar</span><h1>{{ $instructor->name }}</h1><p>{{ $instructor->instructorProfile?->headline }}</p><div class="path-meta big"><span>★ {{ $instructor->instructorProfile?->rating ?? 0 }}</span><span>{{ $instructor->instructorProfile?->years_experience ?? 0 }} tahun pengalaman</span><span>{{ $instructor->instructorProfile?->students_count ?? 0 }} peserta</span>@if($instructor->instructorProfile?->is_verified)<span>✓ Terverifikasi</span>@endif</div></div></div></section><section class="section"><div class="container"><div class="content-card"><h2>Tentang pengajar</h2><p>{{ $instructor->instructorProfile?->bio }}</p><p><strong>Keahlian:</strong> {{ $instructor->instructorProfile?->expertise }}</p><p><strong>Pendidikan:</strong> {{ $instructor->instructorProfile?->education }}</p></div><div class="section-heading"><span class="eyebrow">Course</span><h2>Course dari {{ $instructor->name }}</h2></div><div class="course-market-grid">@foreach($instructor->coursesTaught as $path)<article class="market-course-card"><div class="course-thumb"><span>{{ $path->icon }}</span></div><div class="course-card-body"><h2>{{ $path->title }}</h2><p>{{ $path->description }}</p><div class="course-price-row">@if($path->is_free)<strong>Gratis</strong>@else<strong>Rp{{ number_format($path->effectivePrice(),0,',','.') }}</strong>@endif</div><a class="btn btn-dark btn-full" href="{{ route('courses.show',$path) }}">Lihat Course</a></div></article>@endforeach</div></div></section>@endsection
