@extends('layouts.app')
@section('title','Kelas Saya | SKILLPATH')
@section('content')
<section class="dashboard-hero"><div class="container dashboard-hero-grid"><div><span class="eyebrow">Kelas Saya</span><h1>Jadwal kegiatan {{ $child->name }}.</h1><p>Pantau kelas yang sudah terdaftar, kursi yang dipesan, dan sesi yang pernah dihadiri.</p></div><a class="btn btn-ghost-light" href="{{ route('class-schedules.index') }}">Jadwal Kelas</a></div></section>
<section class="section dashboard-section"><div class="container"><div class="course-market-grid">
@forelse($courses as $item)@php($c=$item['course'])<article class="market-course-card enrolled-card"><div class="course-thumb"><span>{{ $c->icon }}</span><small>{{ $c->classTypeLabel() }}</small></div><div class="course-card-body"><span class="path-skill">{{ $c->skill->name }}</span><h2>{{ $c->title }}</h2><p>Pengajar: {{ $c->instructor?->name }}</p><div class="path-meta"><span>{{ $item['attended_count'] }} sesi dihadiri</span><span>{{ $item['booked_count'] }} jadwal dipesan</span></div>@if($item['next_session'])<div class="content-card"><small>Jadwal berikutnya</small><strong>{{ $item['next_session']->starts_at->translatedFormat('d M Y, H:i') }}</strong><p>📍 {{ $item['next_session']->venue_name }}</p></div>@else<p>Belum ada jadwal mendatang.</p>@endif<a class="btn btn-dark btn-full" href="{{ route('class-schedules.index',['course'=>$c->id]) }}">Pilih / Lihat Jadwal</a>@if($item['certificate'])<a class="card-link center-link" href="{{ route('certificates.show',$c) }}">Lihat sertifikat</a>@endif</div></article>@empty<div class="empty-card"><h2>Belum ada kelas aktif.</h2><a class="btn btn-dark" href="{{ route('explore.index') }}">Cari Kelas Offline</a></div>@endforelse
</div></div></section>
@endsection
