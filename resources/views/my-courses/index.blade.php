@extends('layouts.app')
@section('title','Kelas Saya | SKILLPATH')
@section('content')<section class="dashboard-hero"><div class="container dashboard-hero-grid"><div><span class="eyebrow">Kelas Saya</span><h1>Halo, {{ $child->name }}.</h1><p>Lihat kelas yang sudah terdaftar, jadwal, dan kehadiran.</p></div><a class="btn btn-ghost-light" href="{{ route('live.index') }}">Jadwal Kelas</a></div></section><section class="section dashboard-section"><div class="container"><div class="course-market-grid">@forelse($courses as $item)@php($c=$item['course'])<article class="market-course-card enrolled-card"><div class="course-thumb"><span>{{ $c->icon }}</span></div><div class="course-card-body"><span class="path-skill">{{ $c->skill->name }}</span><h2>{{ $c->title }}</h2><p>Pengajar: {{ $c->instructor?->name }}</p><div class="progress-line"><div><span>Kehadiran</span><strong>{{ $item['progress'] }}%</strong></div><div class="progress-track"><span style="width:{{ $item['progress'] }}%"></span></div></div><a class="btn btn-dark btn-full" href="{{ route('live.index',['course'=>$c->id]) }}">{{ $item['next_session']?'Lihat Jadwal':'Cek Jadwal' }}</a>@if($item['certificate'] && $c->certificate_enabled)<a class="card-link center-link" href="{{ route('certificates.show',$c) }}">Lihat sertifikat</a>@endif</div></article>@empty<div class="empty-card"><h2>Belum ada kelas aktif.</h2><a class="btn btn-dark" href="{{ route('explore.index') }}">Cari Kelas</a></div>@endforelse</div></div></section>@endsection
